# Security policy

Use GitHub's private vulnerability reporting for this repository. Include the
affected commit, reproduction steps, and impact; do not publish exploitable
details in an issue.

`boopackage` is an educational package with no runtime dependencies and is not
published as a production service. Reports involving the build backend,
distribution contents, dependency confusion, or unsafe installation behavior
are still in scope. The supported branch is the current `master` branch.
